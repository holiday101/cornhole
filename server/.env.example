PORT=4001
DB_PATH=/data/cornhole.db
SESSION_TTL_DAYS=30

# Optional: without this, invite emails are skipped (logged, not sent) -
# everything else still works. Get a key at https://resend.com.
RESEND_API_KEY=
EMAIL_FROM="Cornhole Golf <onboarding@resend.dev>"
